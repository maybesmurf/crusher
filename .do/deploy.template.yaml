spec:
  name: crusher
  services:
    - http_port: 3000
      dockerfile_path: Dockerfile
      github: 
        repo: crusherdev/crusher
        branch: crusher-auto-hover
      instance_count: 1
      instance_size_slug: basic-s
      name: crusher-app
      routes:
        - path: /
      source_dir: /
